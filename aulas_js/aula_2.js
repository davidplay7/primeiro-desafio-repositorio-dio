const numero = 2;

const numeroPar = (numero % 2) === 0;

/*
=== pra fazer as comparacoes geralmente
== tenta converter o tipo da variavel
= atribuir receber o valor 
*/
if (numeroPar) {
  console.log(numeroPar);
}